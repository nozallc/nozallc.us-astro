---
import RootLayout from '../../../layouts/RootLayout.astro';
---

<RootLayout
	title="Contacto - NOZA LLC - Diseño Web & Marketing Digital en Lexington, KY"
	description="Ponte en contacto con NOZA LLC para servicios de marketing digital, diseño web, branding o consultoría en Lexington, Kentucky."
	lang="es"
	alternateUrl="/contact"
>
	<!-- Hero Section -->
	<section class="contact-hero">
		<div class="cosmic-background">
			<div class="stars"></div>
			<div class="nebula nebula-1"></div>
			<div class="nebula nebula-2"></div>
			<div class="nebula nebula-3"></div>
		</div>

		<div class="particles"></div>

		<div class="hero-content">
			<h1 class="contact-headline">Hablemos</h1>
			<p class="contact-subheadline">¿Tienes un proyecto en mente? ¿Preguntas sobre nuestros servicios? ¿O simplemente quieres charlar? Nos encantaría escucharte. Ponte en contacto y comencemos una conversación.</p>
		</div>
	</section>

	<!-- Contact Options Section -->
	<section class="contact-options">
		<div class="options-container">
			<div class="options-grid">
				<div class="option-card">
					<div class="option-icon">📞</div>
					<h3>Llámanos</h3>
					<p>¿Tienes una pregunta rápida? Llámanos.</p>
					<a href="tel:+1-859-452-8415" class="option-link">+1 (859) 452-8415</a>
				</div>

				<div class="option-card">
					<div class="option-icon">📍</div>
					<h3>Visítanos</h3>
					<p>Ubicados en Lexington, Kentucky. Sirviendo el centro de KY y más allá.</p>
					<p class="location-text">Lexington, KY 40507</p>
				</div>

				<div class="option-card">
					<div class="option-icon">⏰</div>
					<h3>Horario</h3>
					<p>Lunes - Viernes: 9am - 5pm CT</p>
					<p class="hours-text">Sábado y domingo: Cerrado</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Local SEO Intro Block -->
	<section class="local-signal-intro">
		<div class="intro-container">
			<h2 class="intro-title">Contacta a NOZA LLC en Lexington, KY</h2>
			<p class="intro-description">Servimos a Central Kentucky y más allá. Ya seas una startup, un negocio establecido o un contratista, nuestro equipo está listo para ayudarte a tener éxito en línea.</p>
			<div class="intro-highlights">
				<div class="highlight-item">
					<span class="highlight-icon">✓</span>
					<span>Expertos digitales basados en Lexington</span>
				</div>
				<div class="highlight-item">
					<span class="highlight-icon">✓</span>
					<span>Garantía de respuesta en 24 horas</span>
				</div>
				<div class="highlight-item">
					<span class="highlight-icon">✓</span>
					<span>Llamada de descubrimiento gratuita incluida</span>
				</div>
			</div>
		</div>
	</section>

	<!-- Contact Form Section -->
	<section class="contact-form-section">
		<div class="form-container">
			<div class="form-wrapper">
				<div class="form-header">
					<h2 class="form-title">Envíanos un Mensaje</h2>
					<p class="form-subtitle">Completa el formulario a continuación y nos pondremos en contacto dentro de 24 horas</p>
				</div>

				<!-- Success Message -->
				<div class="form-message form-success" id="successMessage" style="display: none;">
					<span class="message-icon">✓</span>
					<span class="message-text">Gracias — recibimos tu mensaje. Nos comunicaremos pronto.</span>
				</div>

				<!-- Error Message -->
				<div class="form-message form-error" id="errorMessage" style="display: none;">
					<span class="message-icon">⚠</span>
					<span class="message-text" id="errorText">Ocurrió un error. Por favor, inténtalo de nuevo.</span>
				</div>

				<form class="contact-form" id="contactForm" method="POST" action="https://formspree.io/f/myzojzzw">
					<!-- Email (Required) -->
					<div class="form-group">
						<label for="email">Correo Electrónico <span class="required-asterisk">*</span></label>
						<input 
							type="email" 
							id="email" 
							name="email" 
							required 
							placeholder="tu@email.com"
							autocomplete="email"
						>
					</div>

					<!-- Full Name -->
					<div class="form-group">
						<label for="name">Nombre Completo</label>
						<input 
							type="text" 
							id="name" 
							name="name" 
							placeholder="Juan Pérez"
							autocomplete="name"
						>
					</div>

					<!-- Phone -->
					<div class="form-group">
						<label for="phone">Número de Teléfono</label>
						<input 
							type="tel" 
							id="phone" 
							name="phone" 
							placeholder="(859) 555-0000"
							autocomplete="tel"
						>
					</div>

					<!-- Service Interest -->
					<div class="form-group">
						<label for="service">Servicio de Interés</label>
						<select id="service" name="service">
							<option value="">-- Selecciona un servicio --</option>
							<option value="Websites">Sitios Web</option>
							<option value="Branding">Branding</option>
							<option value="Digital Marketing">Marketing Digital</option>
							<option value="Photo & Video">Foto y Vídeo</option>
							<option value="Consulting">Consultoría</option>
							<option value="Vendor Network">Red de Proveedores</option>
						</select>
					</div>

					<!-- Budget Range -->
					<div class="form-group">
						<label for="budget">Rango de Presupuesto</label>
						<select id="budget" name="budget">
							<option value="">-- Selecciona un presupuesto --</option>
							<option value="Under $5,000">Menos de $5,000</option>
							<option value="$5,000 - $15,000">$5,000 - $15,000</option>
							<option value="$15,000 - $50,000">$15,000 - $50,000</option>
							<option value="$50,000 - $100,000">$50,000 - $100,000</option>
							<option value="$100,000+">$100,000+</option>
							<option value="Not sure yet">Aún no estoy seguro</option>
						</select>
					</div>

					<!-- Timeline -->
					<div class="form-group">
						<label for="timeline">Cronograma</label>
						<select id="timeline" name="timeline">
							<option value="">-- Selecciona un cronograma --</option>
							<option value="ASAP">Lo Antes Posible</option>
							<option value="1–2 weeks">1–2 semanas</option>
							<option value="This month">Este mes</option>
							<option value="Next 1–3 months">Próximos 1–3 meses</option>
							<option value="Flexible">Flexible</option>
						</select>
					</div>

					<!-- Message (Required) -->
					<div class="form-group">
						<label for="message">Cuéntanos sobre tu proyecto <span class="required-asterisk">*</span></label>
						<textarea 
							id="message" 
							name="message" 
							rows="5" 
							required 
							placeholder="¿Qué está en tu mente? ¿Qué esperas lograr?"
						></textarea>
					</div>

					<button type="submit" class="form-submit" id="submitButton">
						<span class="button-text">Enviar Mensaje</span>
						<span class="button-loader" style="display: none;">
							<span class="loader-dot"></span>
							<span class="loader-dot"></span>
							<span class="loader-dot"></span>
						</span>
					</button>
				</form>
			</div>

			<!-- Form Sidebar -->
			<div class="form-sidebar">
				<div class="sidebar-card">
					<h3>Llamada de Descubrimiento Rápida</h3>
					<p>¿Prefieres chatear? Reserva una llamada de descubrimiento de 30 minutos para discutir tu proyecto, objetivos y cómo podemos ayudarte.</p>
					<button class="cta-secondary">Programar Llamada</button>
				</div>

				<div class="sidebar-card">
					<h3>Consulta por Correo</h3>
					<p>Envíanos un correo directo con detalles sobre tu proyecto y responderemos dentro de 24 horas.</p>
					<a href="mailto:hello@nozallc.us" class="email-link">hello@nozallc.us</a>
				</div>

				<div class="sidebar-card">
					<h3>Ubicados en Lexington</h3>
					<p>Estamos enraizados en Central Kentucky y apasionados por ayudar a los negocios locales a tener éxito en línea.</p>
					<p class="local-text">Sirviendo a Lexington y Central Kentucky</p>
				</div>
			</div>
		</div>
	</section>

	<!-- FAQ Section -->
	<section class="contact-faq">
		<div class="faq-header">
			<h2 class="faq-title">Preguntas de Contacto</h2>
			<p class="faq-subtitle">Preguntas frecuentes sobre cómo ponerse en contacto</p>
		</div>

		<div class="faq-container">
			<details class="faq-item">
				<summary class="faq-question">¿Cuál es la mejor manera de contactarte?</summary>
				<div class="faq-answer">
					<p>Respondemos a todas las consultas dentro de 24 horas. Utiliza el método que mejor te funcione: completa el formulario de contacto, llámanos, envía un correo o programa una llamada de descubrimiento. Todos son igualmente efectivos.</p>
				</div>
			</details>

			<details class="faq-item">
				<summary class="faq-question">¿Cuándo escucharé de ustedes?</summary>
				<div class="faq-answer">
					<p>Nuestro objetivo es responder a todas las consultas dentro de 24 horas hábiles. Durante períodos ocupados, las respuestas pueden tardar hasta 48 horas. Confirmaremos la recepción de tu mensaje y programaremos una hora para discutir tu proyecto.</p>
				</div>
			</details>

			<details class="faq-item">
				<summary class="faq-question">¿Ofrecen consultas gratuitas?</summary>
				<div class="faq-answer">
					<p>¡Sí! Estamos felices de discutir tu proyecto, responder preguntas y explorar si somos una buena opción para ti. La mayoría de las llamadas iniciales de descubrimiento son gratuitas para que podamos entender tus necesidades y que conozcas a nuestro equipo.</p>
				</div>
			</details>

			<details class="faq-item">
				<summary class="faq-question">¿Atienden clientes fuera de Lexington?</summary>
				<div class="faq-answer">
					<p>Estamos ubicados en Lexington y atendemos principalmente a clientes de Central Kentucky. Sin embargo, también trabajamos con clientes en todo Kentucky y más allá. Los servicios digitales ya no están limitados por geografía. ¡Si estás interesado en trabajar con nosotros, ponte en contacto!</p>
				</div>
			</details>

			<details class="faq-item">
				<summary class="faq-question">¿Qué pasa si no estoy seguro de qué servicio necesito?</summary>
				<div class="faq-answer">
					<p>Eso es completamente normal. Cuéntanos sobre tu negocio y qué esperas lograr. Recomendaremos los servicios adecuados para tu situación. Es mejor discutir tus necesidades que adivinar.</p>
				</div>
			</details>
		</div>
	</section>

	<!-- Final CTA Section -->
	<section class="final-cta">
		<div class="cta-content">
			<h2 class="cta-title">¿Qué Estás Esperando?</h2>
			<p class="cta-description">Ya sea que estés listo para iniciar un proyecto o simplemente quieras explorar opciones, conectémonos. Nuestro equipo está emocionado de ayudar a tu negocio de Lexington a tener éxito en línea.</p>
			<div class="cta-buttons">
				<button class="cta-primary cta-large" onclick="document.getElementById('contactForm').scrollIntoView({ behavior: 'smooth' })">Iniciar Conversación</button>
				<button class="cta-secondary cta-large">Programar Llamada</button>
			</div>
		</div>
	</section>
</RootLayout>

<script>
	// Handle form submission with Formspree
	const contactForm = document.getElementById('contactForm');
	const successMessage = document.getElementById('successMessage');
	const errorMessage = document.getElementById('errorMessage');
	const errorText = document.getElementById('errorText');
	const submitButton = document.getElementById('submitButton');
	const buttonText = submitButton.querySelector('.button-text');
	const buttonLoader = submitButton.querySelector('.button-loader');

	if (contactForm) {
		contactForm.addEventListener('submit', async (e) => {
			e.preventDefault();

			// Validate required fields
			const email = document.getElementById('email').value.trim();
			const message = document.getElementById('message').value.trim();

			if (!email || !message) {
				errorText.textContent = 'Por favor, completa todos los campos requeridos.';
				errorMessage.style.display = 'flex';
				successMessage.style.display = 'none';
				return;
			}

			// Validate email format
			const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
			if (!emailRegex.test(email)) {
				errorText.textContent = 'Por favor, ingresa una dirección de correo válida.';
				errorMessage.style.display = 'flex';
				successMessage.style.display = 'none';
				return;
			}

			// Show loading state
			submitButton.disabled = true;
			buttonText.style.display = 'none';
			buttonLoader.style.display = 'flex';

			try {
				const formData = new FormData(contactForm);
				const response = await fetch('https://formspree.io/f/myzojzzw', {
					method: 'POST',
					body: formData,
					headers: {
						Accept: 'application/json',
					},
				});

				if (response.ok) {
					// Success
					successMessage.style.display = 'flex';
					errorMessage.style.display = 'none';
					contactForm.reset();

					// Scroll to success message
					successMessage.scrollIntoView({ behavior: 'smooth', block: 'center' });

					// Hide success message after 5 seconds and reset button
					setTimeout(() => {
						successMessage.style.display = 'none';
						submitButton.disabled = false;
						buttonText.style.display = 'inline';
						buttonLoader.style.display = 'none';
					}, 5000);
				} else {
					// Error response
					errorText.textContent = 'Error al enviar el mensaje. Por favor, inténtalo de nuevo.';
					errorMessage.style.display = 'flex';
					successMessage.style.display = 'none';

					// Reset button
					submitButton.disabled = false;
					buttonText.style.display = 'inline';
					buttonLoader.style.display = 'none';
				}
			} catch (error) {
				console.error('Form submission error:', error);
				errorText.textContent = 'Ocurrió un error. Por favor, verifica tu conexión e inténtalo de nuevo.';
				errorMessage.style.display = 'flex';
				successMessage.style.display = 'none';

				// Reset button
				submitButton.disabled = false;
				buttonText.style.display = 'inline';
				buttonLoader.style.display = 'none';
			}
		});
	}
</script>

<style>
	/* Hero Section */
	.contact-hero {
		position: relative;
		min-height: 40vh;
		display: flex;
		align-items: center;
		justify-content: center;
		overflow: hidden;
		padding: 4rem 2rem;
	}

	.cosmic-background {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: 0;
		overflow: hidden;
	}

	.stars {
		position: absolute;
		width: 2px;
		height: 2px;
		background: white;
		border-radius: 50%;
		box-shadow: 100px 100px white, 200px 150px white, 300px 50px white, 450px 200px white,
					550px 100px white, 150px 300px white, 400px 400px white, 600px 300px white,
					700px 150px white, 800px 400px white, 900px 250px white, 200px 500px white,
					700px 500px white, 500px 600px white, 100px 700px white, 800px 700px white,
					300px 800px white;
		animation: twinkle 3s infinite;
	}


	.nebula {
		position: absolute;
		border-radius: 50%;
		filter: blur(80px);
		opacity: 0.6;
	}

	.nebula-1 {
		width: 400px;
		height: 400px;
		background: radial-gradient(circle, rgba(0, 212, 255, 0.3) 0%, transparent 70%);
		top: 10%;
		left: 10%;
		animation: drift 8s ease-in-out infinite;
	}

	.nebula-2 {
		width: 500px;
		height: 500px;
		background: radial-gradient(circle, rgba(163, 0, 255, 0.2) 0%, transparent 70%);
		top: 50%;
		right: 5%;
		animation: drift 10s ease-in-out infinite reverse;
	}

	.nebula-3 {
		width: 350px;
		height: 350px;
		background: radial-gradient(circle, rgba(255, 0, 110, 0.2) 0%, transparent 70%);
		bottom: 10%;
		left: 50%;
		animation: drift 12s ease-in-out infinite;
	}


	.particles {
		position: absolute;
		width: 100%;
		height: 100%;
		overflow: hidden;
	}

	.particles::before {
		content: '';
		position: absolute;
		width: 2px;
		height: 2px;
		background: var(--primary-neon);
		border-radius: 50%;
		box-shadow: 100px 200px var(--primary-neon), 250px 100px var(--primary-neon),
					150px 400px var(--secondary-neon), 450px 150px var(--tertiary-neon),
					600px 250px var(--primary-neon), 350px 300px var(--secondary-neon),
					800px 100px var(--tertiary-neon), 200px 600px var(--primary-neon);
		animation: float 6s ease-in-out infinite;
	}


	.hero-content {
		position: relative;
		z-index: 1;
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		max-width: 900px;
		gap: 1.5rem;
		animation: fade-in-up 1s ease-out;
	}

	.contact-headline {
		font-size: clamp(2.5rem, 6vw, 3.5rem);
		font-weight: 800;
		background: linear-gradient(135deg, var(--primary-neon), var(--secondary-neon), var(--tertiary-neon));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		line-height: 1.2;
	}

	.contact-subheadline {
		font-size: clamp(1rem, 2vw, 1.2rem);
		color: var(--text-secondary);
		max-width: 700px;
		line-height: 1.8;
	}


	/* Contact Options Section */
	.contact-options {
		position: relative;
		padding: 4rem 2rem;
		background: linear-gradient(180deg, var(--dark-bg) 0%, rgba(15, 24, 53, 0.8) 100%);
		z-index: 2;
	}

	.options-container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.options-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
	}

	.option-card {
		padding: 2rem;
		background: rgba(0, 212, 255, 0.05);
		border: 1px solid rgba(0, 212, 255, 0.15);
		border-radius: 12px;
		text-align: center;
		transition: all 0.3s ease;
		position: relative;
		overflow: hidden;
	}

	.option-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, rgba(0, 212, 255, 0.1), transparent);
		transition: left 0.6s ease;
		z-index: 0;
	}

	.option-card:hover::before {
		left: 100%;
	}

	.option-card:hover {
		background: rgba(0, 212, 255, 0.12);
		border-color: var(--primary-neon);
		transform: translateY(-8px);
		box-shadow: 0 12px 32px rgba(0, 212, 255, 0.25);
	}

	.option-icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	.option-card h3 {
		font-size: 1.2rem;
		color: var(--text-primary);
		margin-bottom: 0.75rem;
		font-weight: 700;
	}

	.option-card p {
		color: var(--text-secondary);
		font-size: 0.95rem;
		line-height: 1.6;
		margin-bottom: 0.75rem;
	}

	.option-link {
		display: inline-block;
		color: var(--primary-neon);
		font-weight: 600;
		text-decoration: none;
		transition: all 0.3s ease;
	}

	.option-link:hover {
		text-decoration: underline;
	}

	.location-text,
	.hours-text {
		color: var(--primary-neon) !important;
		font-weight: 600 !important;
	}

	/* Local Signal Intro Section */
	.local-signal-intro {
		position: relative;
		padding: 6rem 2rem;
		background: linear-gradient(180deg, rgba(15, 24, 53, 0.8) 0%, var(--dark-bg) 100%);
		z-index: 2;
	}

	.intro-container {
		max-width: 900px;
		margin: 0 auto;
		text-align: center;
	}

	.intro-title {
		font-size: clamp(2rem, 6vw, 3rem);
		background: linear-gradient(135deg, var(--primary-neon), var(--secondary-neon));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin-bottom: 1rem;
		font-weight: 800;
		line-height: 1.2;
	}

	.intro-description {
		font-size: 1.1rem;
		color: var(--text-secondary);
		margin-bottom: 3rem;
		line-height: 1.8;
	}

	.intro-highlights {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
		gap: 1.5rem;
	}

	.highlight-item {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.75rem;
		padding: 1.25rem;
		background: rgba(0, 212, 255, 0.05);
		border: 1px solid rgba(0, 212, 255, 0.15);
		border-radius: 12px;
		backdrop-filter: blur(8px);
		transition: all 0.3s ease;
		position: relative;
		overflow: hidden;
	}

	.highlight-item::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, rgba(0, 212, 255, 0.05), transparent);
		transition: left 0.6s ease;
		z-index: 0;
	}

	.highlight-item:hover::before {
		left: 100%;
	}

	.highlight-item:hover {
		background: rgba(0, 212, 255, 0.12);
		border-color: var(--primary-neon);
		transform: translateY(-8px);
		box-shadow: 0 8px 24px rgba(0, 212, 255, 0.2);
	}

	.highlight-icon {
		color: var(--primary-neon);
		font-weight: 700;
		font-size: 1.3rem;
		flex-shrink: 0;
		position: relative;
		z-index: 1;
	}

	.highlight-item span:last-child {
		color: var(--text-secondary);
		font-size: 0.95rem;
		position: relative;
		z-index: 1;
	}

	/* Contact Form Section */
	.contact-form-section {
		position: relative;
		padding: 6rem 2rem;
		background: linear-gradient(180deg, var(--dark-bg) 0%, rgba(15, 24, 53, 0.8) 100%);
		z-index: 2;
	}

	.form-container {
		max-width: 1200px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1fr 350px;
		gap: 3rem;
		align-items: start;
	}

	.form-wrapper {
		padding: 3rem;
		background: rgba(0, 212, 255, 0.05);
		border: 1px solid rgba(0, 212, 255, 0.15);
		border-radius: 16px;
		backdrop-filter: blur(10px);
		transition: all 0.3s ease;
		position: relative;
		overflow: hidden;
	}

	.form-wrapper::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: linear-gradient(135deg, rgba(0, 212, 255, 0.05) 0%, transparent 100%);
		opacity: 0;
		transition: opacity 0.3s ease;
		pointer-events: none;
		z-index: 0;
	}

	.form-wrapper:hover::before {
		opacity: 1;
	}

	.form-header {
		margin-bottom: 2rem;
		position: relative;
		z-index: 1;
	}

	.form-title {
		font-size: 1.8rem;
		color: var(--text-primary);
		margin-bottom: 0.5rem;
		font-weight: 800;
	}

	.form-subtitle {
		color: var(--text-secondary);
		font-size: 0.95rem;
	}

	/* Form Messages */
	.form-message {
		padding: 1.25rem;
		border-radius: 10px;
		margin-bottom: 2rem;
		display: flex;
		align-items: center;
		gap: 1rem;
		animation: slide-in 0.3s ease-out;
	}


	.form-success {
		background: rgba(76, 175, 80, 0.15);
		border: 1px solid rgba(76, 175, 80, 0.4);
		color: #81c784;
	}

	.form-error {
		background: rgba(244, 67, 54, 0.15);
		border: 1px solid rgba(244, 67, 54, 0.4);
		color: #ef5350;
	}

	.message-icon {
		font-size: 1.3rem;
		font-weight: 700;
		flex-shrink: 0;
	}

	.message-text {
		font-size: 0.95rem;
		line-height: 1.5;
	}

	/* Contact Form */
	.contact-form {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		position: relative;
		z-index: 1;
	}

	.form-group {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.form-group label {
		color: var(--text-primary);
		font-weight: 600;
		font-size: 0.95rem;
	}

	.required-asterisk {
		color: var(--secondary-neon);
	}

	.form-group input,
	.form-group select,
	.form-group textarea {
		padding: 0.875rem 1rem;
		background: rgba(0, 212, 255, 0.03);
		border: 1px solid rgba(0, 212, 255, 0.15);
		border-radius: 10px;
		color: var(--text-primary);
		font-family: inherit;
		font-size: 0.95rem;
		transition: all 0.3s ease;
	}

	.form-group input:focus,
	.form-group select:focus,
	.form-group textarea:focus {
		outline: none;
		background: rgba(0, 212, 255, 0.08);
		border-color: var(--primary-neon);
		box-shadow: 0 0 15px rgba(0, 212, 255, 0.2), inset 0 0 10px rgba(0, 212, 255, 0.05);
	}

	.form-group textarea {
		resize: vertical;
		min-height: 120px;
	}

	/* Form Submit Button */
	.form-submit {
		padding: 1rem 2rem;
		background: linear-gradient(135deg, var(--primary-neon), var(--tertiary-neon));
		color: var(--darker-bg);
		border: none;
		border-radius: 10px;
		font-weight: 700;
		font-size: 1rem;
		cursor: pointer;
		transition: all 0.3s ease;
		box-shadow: 0 0 20px rgba(0, 212, 255, 0.4);
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
		overflow: hidden;
		z-index: 1;
	}

	.form-submit::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
		transition: left 0.6s ease;
	}

	.form-submit:hover:not(:disabled)::before {
		left: 100%;
	}

	.form-submit:hover:not(:disabled) {
		transform: translateY(-2px) scale(1.02);
		box-shadow: 0 8px 32px rgba(0, 212, 255, 0.5), 0 0 60px var(--tertiary-neon);
	}

	.form-submit:disabled {
		opacity: 0.7;
		cursor: not-allowed;
	}

	.button-text {
		display: inline;
		position: relative;
		z-index: 1;
	}

	.button-loader {
		display: flex;
		gap: 0.35rem;
		align-items: center;
		justify-content: center;
		position: relative;
		z-index: 1;
	}

	.loader-dot {
		width: 6px;
		height: 6px;
		background: currentColor;
		border-radius: 50%;
		animation: pulse 1.4s ease-in-out infinite;
	}

	.loader-dot:nth-child(2) {
		animation-delay: 0.2s;
	}

	.loader-dot:nth-child(3) {
		animation-delay: 0.4s;
	}


	/* Form Sidebar */
	.form-sidebar {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.sidebar-card {
		padding: 1.5rem;
		background: rgba(0, 212, 255, 0.05);
		border: 1px solid rgba(0, 212, 255, 0.15);
		border-radius: 12px;
		backdrop-filter: blur(8px);
		transition: all 0.3s ease;
		position: relative;
		overflow: hidden;
	}

	.sidebar-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, rgba(0, 212, 255, 0.08), transparent);
		transition: left 0.6s ease;
		z-index: 0;
	}

	.sidebar-card:hover::before {
		left: 100%;
	}

	.sidebar-card:hover {
		background: rgba(0, 212, 255, 0.12);
		border-color: var(--primary-neon);
		transform: translateY(-8px);
		box-shadow: 0 12px 32px rgba(0, 212, 255, 0.2);
	}

	.sidebar-card h3 {
		font-size: 1rem;
		color: var(--text-primary);
		margin-bottom: 0.75rem;
		font-weight: 700;
		position: relative;
		z-index: 1;
	}

	.sidebar-card p {
		font-size: 0.85rem;
		color: var(--text-secondary);
		line-height: 1.5;
		margin-bottom: 0.75rem;
		position: relative;
		z-index: 1;
	}

	.sidebar-card .cta-secondary {
		width: 100%;
		padding: 0.75rem 1rem;
		font-size: 0.9rem;
		position: relative;
		z-index: 1;
	}

	.email-link {
		display: inline-block;
		color: var(--primary-neon);
		font-weight: 600;
		text-decoration: none;
		word-break: break-all;
		transition: all 0.3s ease;
	}

	.email-link:hover {
		text-decoration: underline;
	}

	.local-text {
		color: var(--primary-neon) !important;
		font-weight: 600 !important;
	}

	/* FAQ Section */
	.contact-faq {
		position: relative;
		padding: 6rem 2rem;
		background: linear-gradient(180deg, var(--dark-bg) 0%, rgba(15, 24, 53, 0.8) 100%);
		z-index: 2;
	}

	.faq-header {
		text-align: center;
		margin-bottom: 3rem;
		max-width: 800px;
		margin-left: auto;
		margin-right: auto;
	}

	.faq-title {
		font-size: clamp(1.8rem, 5vw, 2.5rem);
		background: linear-gradient(135deg, var(--secondary-neon), var(--primary-neon));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin-bottom: 0.5rem;
		font-weight: 800;
	}

	.faq-subtitle {
		font-size: 1.1rem;
		color: var(--text-secondary);
	}

	.faq-container {
		max-width: 900px;
		margin: 0 auto;
	}

	.faq-item {
		margin-bottom: 1.5rem;
		border-radius: 12px;
		overflow: hidden;
		background: rgba(0, 212, 255, 0.05);
		border: 1px solid rgba(0, 212, 255, 0.15);
		transition: all 0.3s ease;
		backdrop-filter: blur(8px);
	}

	.faq-item[open] {
		background: rgba(0, 212, 255, 0.12);
		border-color: var(--primary-neon);
		box-shadow: 0 8px 24px rgba(0, 212, 255, 0.15);
	}

	.faq-item:hover {
		border-color: rgba(0, 212, 255, 0.3);
	}

	.faq-question {
		padding: 1.5rem;
		cursor: pointer;
		display: flex;
		justify-content: space-between;
		align-items: center;
		font-weight: 600;
		color: var(--text-primary);
		transition: all 0.3s ease;
		user-select: none;
		font-size: 1rem;
	}

	.faq-item:hover .faq-question {
		color: var(--primary-neon);
	}

	.faq-question::after {
		content: '▼';
		color: var(--primary-neon);
		font-size: 0.8rem;
		transition: transform 0.3s ease;
		flex-shrink: 0;
		margin-left: 1rem;
	}

	.faq-item[open] .faq-question::after {
		transform: rotate(180deg);
	}

	.faq-answer {
		padding: 0 1.5rem 1.5rem;
		animation: expand 0.3s ease-out;
		color: var(--text-secondary);
		line-height: 1.8;
		font-size: 0.95rem;
	}


	/* Final CTA Section */
	.final-cta {
		position: relative;
		padding: 6rem 2rem;
		background: linear-gradient(180deg, rgba(15, 24, 53, 0.8) 0%, var(--dark-bg) 100%);
		z-index: 2;
		min-height: 400px;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.cta-content {
		text-align: center;
		max-width: 700px;
	}

	.cta-title {
		font-size: clamp(1.8rem, 5vw, 2.8rem);
		background: linear-gradient(135deg, var(--tertiary-neon), var(--primary-neon));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin-bottom: 1.5rem;
		font-weight: 800;
	}

	.cta-description {
		font-size: 1.1rem;
		color: var(--text-secondary);
		margin-bottom: 2.5rem;
		line-height: 1.8;
	}

	.cta-buttons {
		display: flex;
		gap: 1.5rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.cta-large {
		padding: 1.2rem 3rem;
		font-size: 1.1rem;
	}

	/* Responsive Design */
	@media (max-width: 768px) {
		.contact-hero {
			min-height: 35vh;
			padding: 3rem 1.5rem;
		}

		.local-signal-intro,
		.contact-options,
		.contact-form-section,
		.contact-faq,
		.final-cta {
			padding: 4rem 1.5rem;
		}

		.form-container {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.form-wrapper {
			padding: 2rem;
		}

		.options-grid {
			gap: 1.5rem;
		}

		.intro-highlights {
			grid-template-columns: 1fr;
		}

		.cta-buttons {
			flex-direction: column;
			gap: 1rem;
		}

		.cta-large {
			width: 100%;
			padding: 1rem 2rem;
		}
	}

	@media (max-width: 480px) {
		.contact-headline {
			font-size: clamp(1.5rem, 5vw, 2.2rem);
		}

		.contact-subheadline {
			font-size: 0.95rem;
		}

		.form-title,
		.faq-title,
		.cta-title,
		.intro-title {
			font-size: clamp(1.5rem, 5vw, 2rem);
		}

		.faq-question {
			padding: 1.25rem;
			font-size: 0.95rem;
		}

		.faq-answer {
			font-size: 0.9rem;
		}

		.form-wrapper,
		.option-card,
		.sidebar-card {
			padding: 1.5rem;
		}

		.intro-description {
			font-size: 1rem;
		}

		.highlight-item {
			font-size: 0.9rem;
		}
	}
</style>
