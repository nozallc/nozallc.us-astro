---
---

<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />
	<link rel="icon" type="image/svg+xml" href="/noza.favicon.svg" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<meta name="generator" content={Astro.generator} />
	<title>NOZA LLC - Digital Marketing, Branding & Web Design in Lexington, KY</title>
	<meta name="description" content="Digital marketing, branding, websites & business consulting for Lexington KY small businesses. Creative workflows powered by Builder.io, GitHub, Astro & Cloudflare." />
	<meta property="og:title" content="NOZA LLC - Digital Marketing & Web Design in Lexington, KY" />
	<meta property="og:description" content="Launch your business into the future with cutting-edge digital marketing, branding, and web design services in Lexington, Kentucky." />
	<meta property="og:type" content="website" />
</head>
<body>
	<!-- Hero Section -->
	<section class="hero">
		<div class="cosmic-background">
			<div class="stars"></div>
			<div class="nebula nebula-1"></div>
			<div class="nebula nebula-2"></div>
			<div class="nebula nebula-3"></div>
		</div>

		<div class="particles"></div>

		<div class="hero-content">
			<div class="glowing-orb-container">
				<div class="glowing-orb"></div>
				<div class="orbit-ring orbit-ring-1"></div>
				<div class="orbit-ring orbit-ring-2"></div>
				<div class="orbit-ring orbit-ring-3"></div>
			</div>

			<div class="hero-text">
				<h1 class="hero-headline">Launch Your Business Into the Future</h1>
				<p class="hero-subheadline">Digital marketing, branding, websites & business consulting for Lexington KY small businesses—powered by a cutting-edge Builder → GitHub → Astro → Cloudflare workflow.</p>
				
				<div class="hero-ctas">
					<button class="cta-primary">Book a Discovery Call</button>
					<button class="cta-secondary">See Our Work</button>
				</div>

				<p class="hero-location">📍 Based in Lexington, Kentucky • Serving Central KY & beyond</p>
			</div>
		</div>
	</section>

	<!-- Services Overview Section -->
	<section class="services">
		<div class="section-header">
			<h2>Our Services</h2>
			<p class="section-subtitle">Creative solutions powered by innovation</p>
		</div>

		<div class="services-grid">
			<div class="service-card">
				<div class="card-icon digital-marketing"></div>
				<h3>Digital Marketing Strategy</h3>
				<p>Data-driven campaigns designed to grow your reach, engage your audience, and drive measurable results for your Lexington business.</p>
			</div>

			<div class="service-card">
				<div class="card-icon branding"></div>
				<h3>Branding & Logo Design</h3>
				<p>Distinctive visual identities that capture your brand essence and resonate with your Central Kentucky market.</p>
			</div>

			<div class="service-card">
				<div class="card-icon web-design"></div>
				<h3>Websites (Astro + Cloudflare)</h3>
				<p>Ultra-fast, SEO-optimized websites built with cutting-edge technology for limitless customization and speed.</p>
			</div>

			<div class="service-card">
				<div class="card-icon social-media"></div>
				<h3>Social Media Campaigns</h3>
				<p>Strategic content creation and community management that builds authentic connections with your audience.</p>
			</div>

			<div class="service-card">
				<div class="card-icon media"></div>
				<h3>Photo, Video & Drone Media</h3>
				<p>Professional media production that showcases your brand story and captivates your viewers.</p>
			</div>

			<div class="service-card">
				<div class="card-icon consulting"></div>
				<h3>Business Consulting</h3>
				<p>Strategic guidance for growth, systems optimization, and sustainable business development in your market.</p>
			</div>
		</div>
	</section>

	<!-- The Stack Section -->
	<section class="stack">
		<div class="section-header">
			<h2>A Modern, Limitless Workflow Built for Speed and Creativity</h2>
			<p class="section-subtitle">The technology that powers exceptional results</p>
		</div>

		<div class="pipeline-container">
			<div class="pipeline-node">
				<div class="node-glow"></div>
				<div class="node-icon builder-icon">B</div>
				<p class="node-label">Builder.io</p>
				<p class="node-desc">Visual design & collaboration</p>
			</div>

			<div class="pipeline-connector">
				<div class="connector-line"></div>
				<span class="connector-arrow">→</span>
			</div>

			<div class="pipeline-node">
				<div class="node-glow"></div>
				<div class="node-icon github-icon">G</div>
				<p class="node-label">GitHub</p>
				<p class="node-desc">Version control & sync</p>
			</div>

			<div class="pipeline-connector">
				<div class="connector-line"></div>
				<span class="connector-arrow">→</span>
			</div>

			<div class="pipeline-node">
				<div class="node-glow"></div>
				<div class="node-icon astro-icon">A</div>
				<p class="node-label">Astro</p>
				<p class="node-desc">Fast framework</p>
			</div>

			<div class="pipeline-connector">
				<div class="connector-line"></div>
				<span class="connector-arrow">→</span>
			</div>

			<div class="pipeline-node">
				<div class="node-glow"></div>
				<div class="node-icon cloudflare-icon">C</div>
				<p class="node-label">Cloudflare Pages</p>
				<p class="node-desc">Lightning-fast hosting</p>
			</div>
		</div>

		<div class="stack-benefits">
			<div class="benefit-item">
				<span class="benefit-icon">✨</span>
				<p><strong>Limitless Customization</strong> — Full creative control with no restrictions</p>
			</div>
			<div class="benefit-item">
				<span class="benefit-icon">⚡</span>
				<p><strong>Extremely Fast Load Speeds</strong> — Sub-second performance globally</p>
			</div>
			<div class="benefit-item">
				<span class="benefit-icon">🔍</span>
				<p><strong>Strong SEO</strong> — Built for search visibility and rankings</p>
			</div>
			<div class="benefit-item">
				<span class="benefit-icon">📝</span>
				<p><strong>Easier Updates</strong> — Simple content management and deployment</p>
			</div>
		</div>
	</section>

	<!-- Portfolio Section -->
	<section class="portfolio">
		<div class="section-header">
			<h2>Real Projects. Real Growth.</h2>
			<p class="section-subtitle">Case studies that showcase our creative power</p>
		</div>

		<div class="portfolio-grid">
			<div class="portfolio-item">
				<div class="portfolio-image branding-case"></div>
				<div class="portfolio-overlay">
					<h3>Brand Identity Redesign</h3>
					<p>Complete visual brand system</p>
				</div>
			</div>

			<div class="portfolio-item">
				<div class="portfolio-image website-case"></div>
				<div class="portfolio-overlay">
					<h3>E-Commerce Site</h3>
					<p>Ultra-fast Astro powered site</p>
				</div>
			</div>

			<div class="portfolio-item">
				<div class="portfolio-image campaign-case"></div>
				<div class="portfolio-overlay">
					<h3>Digital Marketing Campaign</h3>
					<p>Multi-channel strategy & execution</p>
				</div>
			</div>

			<div class="portfolio-item">
				<div class="portfolio-image video-case"></div>
				<div class="portfolio-overlay">
					<h3>Drone & Video Production</h3>
					<p>Professional cinematic content</p>
				</div>
			</div>

			<div class="portfolio-item">
				<div class="portfolio-image rebranding-case"></div>
				<div class="portfolio-overlay">
					<h3>Full Brand Relaunch</h3>
					<p>Website, identity & marketing</p>
				</div>
			</div>

			<div class="portfolio-item">
				<div class="portfolio-image consulting-case"></div>
				<div class="portfolio-overlay">
					<h3>Business Growth Strategy</h3>
					<p>Systems & lead generation</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Consulting Section -->
	<section class="consulting">
		<div class="section-header">
			<h2>Strategy, Systems & Support for Real Small Businesses</h2>
			<p class="section-subtitle">Consulting that delivers sustainable growth</p>
		</div>

		<div class="consulting-cards">
			<div class="consulting-card">
				<div class="card-number">01</div>
				<h3>Startup Guidance</h3>
				<p>From concept to launch—we help new businesses establish their foundation, brand identity, and first-customer acquisition strategy.</p>
			</div>

			<div class="consulting-card">
				<div class="card-number">02</div>
				<h3>Local Business Growth Plans</h3>
				<p>Strategic roadmaps tailored to Lexington and Central Kentucky markets, with actionable steps for expansion and market penetration.</p>
			</div>

			<div class="consulting-card">
				<div class="card-number">03</div>
				<h3>Branding Clarity</h3>
				<p>Define your unique value proposition, messaging, and positioning to stand out in your industry and market.</p>
			</div>

			<div class="consulting-card">
				<div class="card-number">04</div>
				<h3>Lead Systems</h3>
				<p>Build reliable customer acquisition funnels powered by digital marketing, automation, and conversion optimization.</p>
			</div>

			<div class="consulting-card">
				<div class="card-number">05</div>
				<h3>Vendor Network Connections</h3>
				<p>Strategic introductions to trusted partners in our ecosystem—from web developers to business service providers.</p>
			</div>

			<div class="consulting-card">
				<div class="card-number">06</div>
				<h3>Ongoing Support</h3>
				<p>Continuous optimization, strategy refinement, and partnership as your business evolves and scales.</p>
			</div>
		</div>
	</section>

	<!-- Vendor Network Section -->
	<section class="vendor-network">
		<div class="section-header">
			<h2>The NOZA Orbit</h2>
			<p class="section-subtitle">Building a network of trusted partners</p>
		</div>

		<div class="orbit-container">
			<div class="orbit-center">
				<img src="/nozallc.svg" alt="NOZA LLC Logo" class="orbit-logo" />
				<p class="orbit-center-text">NOZA</p>
			</div>

			<div class="orbit-nodes">
				<div class="orbit-node node-1">
					<div class="node-dot"></div>
					<p class="node-name">Cleaning Services</p>
				</div>
				<div class="orbit-node node-2">
					<div class="node-dot"></div>
					<p class="node-name">Contracting</p>
				</div>
				<div class="orbit-node node-3">
					<div class="node-dot"></div>
					<p class="node-name">Landscaping</p>
				</div>
				<div class="orbit-node node-4">
					<div class="node-dot"></div>
					<p class="node-name">Real Estate</p>
				</div>
				<div class="orbit-node node-5">
					<div class="node-dot"></div>
					<p class="node-name">Consulting</p>
				</div>
				<div class="orbit-node node-6">
					<div class="node-dot"></div>
					<p class="node-name">Digital Services</p>
				</div>
			</div>
		</div>

		<div class="vendor-info">
			<p><strong>NOZA connects businesses with trusted vendors in our ecosystem.</strong> We operate on a referral-based revenue model where our partners grow together. When we find the perfect match between a client need and a vendor capability, everyone wins.</p>
			<button class="cta-primary vendor-cta">Apply to Join the NOZA Network</button>
		</div>
	</section>

	<!-- FAQ Section -->
	<section class="faq">
		<div class="section-header">
			<h2>Frequently Asked Questions</h2>
			<p class="section-subtitle">Everything you need to know</p>
		</div>

		<div class="faq-container">
			<details class="faq-item">
				<summary class="faq-question">What digital marketing services do you offer in Lexington KY?</summary>
				<div class="faq-answer">
					<p>We offer a full spectrum of digital marketing services tailored to Lexington small businesses, including SEO optimization, social media management, paid advertising campaigns, content strategy, email marketing, and analytics-driven optimization. All strategies are customized to your local market and business goals.</p>
				</div>
			</details>

			<details class="faq-item">
				<summary class="faq-question">How much does a small business website cost?</summary>
				<div class="faq-answer">
					<p>Website costs vary based on complexity, features, and customization. Typically, small business websites range from foundational Astro-built sites to fully custom experiences. We offer flexible pricing models including project-based fees and performance-based pricing. Contact us for a personalized quote based on your specific needs.</p>
				</div>
			</details>

			<details class="faq-item">
				<summary class="faq-question">Do you work with contractors and service trades?</summary>
				<div class="faq-answer">
					<p>Absolutely! We specialize in serving service-based businesses and trades in Central Kentucky. We understand the unique challenges contractors, cleaners, landscapers, and real estate professionals face, and we've built solutions that generate leads and build credibility in those industries.</p>
				</div>
			</details>

			<details class="faq-item">
				<summary class="faq-question">Why use Astro + Cloudflare?</summary>
				<div class="faq-answer">
					<p>Astro provides superior performance through modern framework architecture, while Cloudflare Pages delivers lightning-fast global hosting with built-in security. Together, they create websites that rank better in search, load in milliseconds, and provide an exceptional user experience—all while keeping costs low and customization unlimited.</p>
				</div>
			</details>

			<details class="faq-item">
				<summary class="faq-question">Do you provide social media content creation?</summary>
				<div class="faq-answer">
					<p>Yes! We provide complete social media services including content strategy, copywriting, graphic design, video production, community management, and campaign execution across Instagram, Facebook, LinkedIn, TikTok, and other relevant platforms for your business.</p>
				</div>
			</details>

			<details class="faq-item">
				<summary class="faq-question">Do you help new businesses get started?</summary>
				<div class="faq-answer">
					<p>Yes! If you're launching a new business or venture, we provide comprehensive startup guidance including business name and brand positioning, visual identity design, website launch, initial marketing strategy, and foundational systems to help you get customers from day one.</p>
				</div>
			</details>
		</div>
	</section>

	<!-- Final CTA Section -->
	<section class="final-cta">
		<div class="cosmic-background-cta">
			<div class="stars-cta"></div>
			<div class="nebula-cta"></div>
		</div>

		<div class="cta-content">
			<h2 class="cta-headline">Ready to Build Something Extraordinary?</h2>
			<p class="cta-subheadline">Let's launch your business into the future.</p>

			<div class="cta-buttons">
				<button class="cta-primary cta-large">Start Your Project</button>
				<button class="cta-secondary cta-large">Book a Call</button>
			</div>
		</div>
	</section>

	<!-- Footer -->
	<footer class="footer">
		<div class="footer-content">
			<div class="footer-col">
				<img src="/nozallc.svg" alt="NOZA LLC" class="footer-logo" />
				<p class="footer-tagline">Digital marketing, branding & web design for Lexington, Kentucky</p>
			</div>

			<div class="footer-col">
				<h4>Services</h4>
				<ul>
					<li><a href="#services">Digital Marketing</a></li>
					<li><a href="#services">Web Design</a></li>
					<li><a href="#services">Branding</a></li>
					<li><a href="#services">Business Consulting</a></li>
				</ul>
			</div>

			<div class="footer-col">
				<h4>Company</h4>
				<ul>
					<li><a href="#portfolio">Our Work</a></li>
					<li><a href="#faq">FAQ</a></li>
					<li><a href="#vendor">Vendor Network</a></li>
					<li><a href="#contact">Contact</a></li>
				</ul>
			</div>

			<div class="footer-col">
				<h4>Connect</h4>
				<ul>
					<li><a href="https://instagram.com/nozallc" target="_blank">Instagram</a></li>
					<li><a href="https://facebook.com/nozallc" target="_blank">Facebook</a></li>
					<li><a href="https://linkedin.com/company/nozallc" target="_blank">LinkedIn</a></li>
					<li><a href="mailto:hello@nozallc.us">Email</a></li>
				</ul>
			</div>
		</div>

		<div class="footer-bottom">
			<p>&copy; 2025 NOZA LLC. All rights reserved. | Based in Lexington, Kentucky</p>
		</div>
	</footer>

	<style>
		:root {
			--primary-neon: #00d4ff;
			--secondary-neon: #ff006e;
			--tertiary-neon: #a300ff;
			--glow-color: #00d4ff;
			--dark-bg: #0a0e27;
			--darker-bg: #05071a;
			--card-bg: rgba(10, 14, 39, 0.6);
			--border-glow: rgba(0, 212, 255, 0.3);
			--text-primary: #e0e0ff;
			--text-secondary: #a0a0c0;
		}

		* {
			margin: 0;
			padding: 0;
			box-sizing: border-box;
		}

		html {
			scroll-behavior: smooth;
		}

		body {
			font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
			background: linear-gradient(135deg, var(--darker-bg) 0%, #0f1835 50%, var(--dark-bg) 100%);
			color: var(--text-primary);
			line-height: 1.6;
			overflow-x: hidden;
		}

		/* Hero Section */
		.hero {
			position: relative;
			min-height: 100vh;
			display: flex;
			align-items: center;
			justify-content: center;
			overflow: hidden;
			padding: 2rem;
		}

		.cosmic-background {
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			z-index: 0;
			overflow: hidden;
		}

		.stars {
			position: absolute;
			width: 2px;
			height: 2px;
			background: white;
			border-radius: 50%;
			box-shadow: 100px 100px white, 200px 150px white, 300px 50px white, 450px 200px white,
						550px 100px white, 150px 300px white, 400px 400px white, 600px 300px white,
						700px 150px white, 800px 400px white, 900px 250px white, 200px 500px white,
						700px 500px white, 500px 600px white, 100px 700px white, 800px 700px white,
						300px 800px white;
			animation: twinkle 3s infinite;
		}

		@keyframes twinkle {
			0%, 100% { opacity: 0.3; }
			50% { opacity: 1; }
		}

		.nebula {
			position: absolute;
			border-radius: 50%;
			filter: blur(80px);
			opacity: 0.6;
		}

		.nebula-1 {
			width: 400px;
			height: 400px;
			background: radial-gradient(circle, rgba(0, 212, 255, 0.3) 0%, transparent 70%);
			top: 10%;
			left: 10%;
			animation: drift 8s ease-in-out infinite;
		}

		.nebula-2 {
			width: 500px;
			height: 500px;
			background: radial-gradient(circle, rgba(163, 0, 255, 0.2) 0%, transparent 70%);
			top: 50%;
			right: 5%;
			animation: drift 10s ease-in-out infinite reverse;
		}

		.nebula-3 {
			width: 350px;
			height: 350px;
			background: radial-gradient(circle, rgba(255, 0, 110, 0.2) 0%, transparent 70%);
			bottom: 10%;
			left: 50%;
			animation: drift 12s ease-in-out infinite;
		}

		@keyframes drift {
			0%, 100% { transform: translate(0, 0); }
			50% { transform: translate(50px, 50px); }
		}

		.particles {
			position: absolute;
			width: 100%;
			height: 100%;
			overflow: hidden;
		}

		.particles::before {
			content: '';
			position: absolute;
			width: 2px;
			height: 2px;
			background: var(--primary-neon);
			border-radius: 50%;
			box-shadow: 100px 200px var(--primary-neon), 250px 100px var(--primary-neon),
						150px 400px var(--secondary-neon), 450px 150px var(--tertiary-neon),
						600px 250px var(--primary-neon), 350px 300px var(--secondary-neon),
						800px 100px var(--tertiary-neon), 200px 600px var(--primary-neon);
			animation: float 6s ease-in-out infinite;
		}

		@keyframes float {
			0%, 100% { transform: translateY(0) translateX(0); opacity: 0.5; }
			50% { transform: translateY(-50px) translateX(30px); opacity: 1; }
		}

		.hero-content {
			position: relative;
			z-index: 1;
			display: flex;
			flex-direction: column;
			align-items: center;
			text-align: center;
			max-width: 900px;
			gap: 3rem;
		}

		.glowing-orb-container {
			position: relative;
			width: 200px;
			height: 200px;
			margin-bottom: 2rem;
		}

		.glowing-orb {
			position: absolute;
			width: 150px;
			height: 150px;
			top: 50%;
			left: 50%;
			transform: translate(-50%, -50%);
			background: radial-gradient(circle at 35% 35%, rgba(0, 212, 255, 0.8), rgba(163, 0, 255, 0.6), rgba(255, 0, 110, 0.4));
			border-radius: 50%;
			filter: blur(30px);
			animation: pulse-glow 3s ease-in-out infinite;
			box-shadow: 0 0 80px var(--primary-neon), 0 0 120px var(--tertiary-neon);
		}

		@keyframes pulse-glow {
			0%, 100% { 
				transform: translate(-50%, -50%) scale(1);
				box-shadow: 0 0 80px var(--primary-neon), 0 0 120px var(--tertiary-neon);
			}
			50% { 
				transform: translate(-50%, -50%) scale(1.2);
				box-shadow: 0 0 120px var(--primary-neon), 0 0 160px var(--tertiary-neon);
			}
		}

		.orbit-ring {
			position: absolute;
			border: 2px solid;
			border-radius: 50%;
			top: 50%;
			left: 50%;
		}

		.orbit-ring-1 {
			width: 200px;
			height: 200px;
			border-color: rgba(0, 212, 255, 0.3);
			transform: translate(-50%, -50%) rotateX(75deg);
			animation: spin-orbit 8s linear infinite;
		}

		.orbit-ring-2 {
			width: 250px;
			height: 250px;
			border-color: rgba(163, 0, 255, 0.2);
			transform: translate(-50%, -50%) rotateX(60deg);
			animation: spin-orbit 10s linear infinite reverse;
		}

		.orbit-ring-3 {
			width: 300px;
			height: 300px;
			border-color: rgba(255, 0, 110, 0.15);
			transform: translate(-50%, -50%) rotateX(45deg);
			animation: spin-orbit 12s linear infinite;
		}

		@keyframes spin-orbit {
			0% { transform: translate(-50%, -50%) rotateX(75deg) rotateZ(0deg); }
			100% { transform: translate(-50%, -50%) rotateX(75deg) rotateZ(360deg); }
		}

		.hero-text {
			display: flex;
			flex-direction: column;
			gap: 1.5rem;
		}

		.hero-headline {
			font-size: clamp(2.5rem, 8vw, 4rem);
			font-weight: 800;
			background: linear-gradient(135deg, var(--primary-neon), var(--secondary-neon), var(--tertiary-neon));
			-webkit-background-clip: text;
			-webkit-text-fill-color: transparent;
			background-clip: text;
			line-height: 1.2;
			animation: fade-in-up 1s ease-out;
		}

		.hero-subheadline {
			font-size: clamp(1rem, 2.5vw, 1.25rem);
			color: var(--text-secondary);
			max-width: 700px;
			margin: 0 auto;
			line-height: 1.8;
			animation: fade-in-up 1s ease-out 0.2s both;
		}

		@keyframes fade-in-up {
			from {
				opacity: 0;
				transform: translateY(30px);
			}
			to {
				opacity: 1;
				transform: translateY(0);
			}
		}

		.hero-ctas {
			display: flex;
			gap: 1.5rem;
			justify-content: center;
			flex-wrap: wrap;
			animation: fade-in-up 1s ease-out 0.4s both;
		}

		.cta-primary {
			padding: 1rem 2.5rem;
			font-size: 1rem;
			font-weight: 600;
			background: linear-gradient(135deg, var(--primary-neon), var(--tertiary-neon));
			color: var(--darker-bg);
			border: none;
			border-radius: 8px;
			cursor: pointer;
			transition: all 0.3s ease;
			box-shadow: 0 0 20px rgba(0, 212, 255, 0.5);
			position: relative;
			overflow: hidden;
		}

		.cta-primary::before {
			content: '';
			position: absolute;
			top: 0;
			left: -100%;
			width: 100%;
			height: 100%;
			background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
			transition: left 0.6s ease;
		}

		.cta-primary:hover::before {
			left: 100%;
		}

		.cta-primary:hover {
			transform: scale(1.05);
			box-shadow: 0 0 40px rgba(0, 212, 255, 0.8), 0 0 60px var(--tertiary-neon);
		}

		.cta-secondary {
			padding: 1rem 2.5rem;
			font-size: 1rem;
			font-weight: 600;
			background: transparent;
			color: var(--primary-neon);
			border: 2px solid var(--primary-neon);
			border-radius: 8px;
			cursor: pointer;
			transition: all 0.3s ease;
			backdrop-filter: blur(10px);
		}

		.cta-secondary:hover {
			background: rgba(0, 212, 255, 0.1);
			box-shadow: 0 0 20px rgba(0, 212, 255, 0.5), inset 0 0 20px rgba(0, 212, 255, 0.1);
			transform: scale(1.05);
		}

		.cta-large {
			padding: 1.2rem 3rem;
			font-size: 1.1rem;
		}

		.hero-location {
			font-size: 0.95rem;
			color: var(--text-secondary);
			animation: fade-in-up 1s ease-out 0.6s both;
		}

		/* Services Section */
		.services {
			position: relative;
			padding: 6rem 2rem;
			background: linear-gradient(180deg, var(--dark-bg) 0%, rgba(15, 24, 53, 0.8) 100%);
			z-index: 2;
		}

		.section-header {
			text-align: center;
			margin-bottom: 4rem;
		}

		.section-header h2 {
			font-size: clamp(2rem, 6vw, 3rem);
			background: linear-gradient(135deg, var(--primary-neon), var(--secondary-neon));
			-webkit-background-clip: text;
			-webkit-text-fill-color: transparent;
			background-clip: text;
			margin-bottom: 1rem;
			font-weight: 800;
		}

		.section-subtitle {
			font-size: 1.1rem;
			color: var(--text-secondary);
		}

		.services-grid {
			display: grid;
			grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
			gap: 2rem;
			max-width: 1200px;
			margin: 0 auto;
		}

		.service-card {
			padding: 2rem;
			background: rgba(0, 212, 255, 0.05);
			border: 1px solid var(--border-glow);
			border-radius: 12px;
			cursor: pointer;
			transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
			position: relative;
			overflow: hidden;
			display: flex;
			flex-direction: column;
			gap: 1rem;
		}

		.service-card::before {
			content: '';
			position: absolute;
			top: 0;
			left: -100%;
			width: 100%;
			height: 100%;
			background: linear-gradient(90deg, transparent, rgba(0, 212, 255, 0.1), transparent);
			transition: left 0.6s ease;
			z-index: 0;
		}

		.service-card:hover::before {
			left: 100%;
		}

		.service-card:hover {
			background: rgba(0, 212, 255, 0.15);
			border-color: var(--primary-neon);
			transform: translateY(-10px);
			box-shadow: 0 0 30px rgba(0, 212, 255, 0.4);
		}

		.card-icon {
			width: 60px;
			height: 60px;
			border-radius: 12px;
			background: linear-gradient(135deg, var(--primary-neon), var(--tertiary-neon));
			position: relative;
			z-index: 1;
		}

		.service-card h3 {
			font-size: 1.3rem;
			position: relative;
			z-index: 1;
			color: var(--text-primary);
		}

		.service-card p {
			color: var(--text-secondary);
			position: relative;
			z-index: 1;
			line-height: 1.6;
		}

		/* Stack Section */
		.stack {
			position: relative;
			padding: 6rem 2rem;
			background: linear-gradient(180deg, rgba(15, 24, 53, 0.8) 0%, var(--dark-bg) 100%);
			z-index: 2;
		}

		.pipeline-container {
			display: flex;
			justify-content: center;
			align-items: center;
			gap: 1rem;
			flex-wrap: wrap;
			max-width: 1200px;
			margin: 3rem auto;
			padding: 3rem 0;
		}

		.pipeline-node {
			display: flex;
			flex-direction: column;
			align-items: center;
			gap: 1rem;
			position: relative;
			min-width: 150px;
		}

		.node-glow {
			position: absolute;
			width: 120px;
			height: 120px;
			border-radius: 50%;
			background: radial-gradient(circle, rgba(0, 212, 255, 0.2), transparent);
			animation: pulse 2s ease-in-out infinite;
		}

		@keyframes pulse {
			0%, 100% { transform: scale(1); opacity: 0.6; }
			50% { transform: scale(1.3); opacity: 1; }
		}

		.node-icon {
			width: 80px;
			height: 80px;
			border-radius: 12px;
			display: flex;
			align-items: center;
			justify-content: center;
			font-size: 2rem;
			font-weight: 700;
			color: white;
			position: relative;
			z-index: 1;
			border: 2px solid var(--primary-neon);
		}

		.builder-icon {
			background: linear-gradient(135deg, #667eea, #764ba2);
		}

		.github-icon {
			background: linear-gradient(135deg, #1a1a2e, #16213e);
		}

		.astro-icon {
			background: linear-gradient(135deg, #ff5a3d, #ffa500);
		}

		.cloudflare-icon {
			background: linear-gradient(135deg, #f38020, #f39c12);
		}

		.node-label {
			font-weight: 700;
			font-size: 1.1rem;
			position: relative;
			z-index: 1;
		}

		.node-desc {
			font-size: 0.9rem;
			color: var(--text-secondary);
			position: relative;
			z-index: 1;
		}

		.pipeline-connector {
			display: flex;
			align-items: center;
			gap: 0.5rem;
			min-width: 60px;
			position: relative;
		}

		.connector-line {
			flex: 1;
			height: 2px;
			background: linear-gradient(90deg, var(--primary-neon), var(--tertiary-neon), transparent);
			position: relative;
		}

		.connector-line::after {
			content: '';
			position: absolute;
			right: 0;
			top: 50%;
			transform: translateY(-50%);
			width: 2px;
			height: 20px;
			background: linear-gradient(90deg, var(--primary-neon), transparent);
			animation: pulse-line 1.5s ease-in-out infinite;
		}

		@keyframes pulse-line {
			0%, 100% { opacity: 0.5; }
			50% { opacity: 1; }
		}

		.connector-arrow {
			color: var(--primary-neon);
			font-size: 1.5rem;
			animation: bounce 1.5s ease-in-out infinite;
		}

		@keyframes bounce {
			0%, 100% { transform: translateX(0); }
			50% { transform: translateX(5px); }
		}

		.stack-benefits {
			display: grid;
			grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
			gap: 2rem;
			max-width: 900px;
			margin: 3rem auto 0;
		}

		.benefit-item {
			text-align: center;
			padding: 1.5rem;
			border-radius: 8px;
			background: rgba(0, 212, 255, 0.05);
			border: 1px solid var(--border-glow);
			transition: all 0.3s ease;
		}

		.benefit-item:hover {
			background: rgba(0, 212, 255, 0.1);
			border-color: var(--primary-neon);
		}

		.benefit-icon {
			font-size: 2rem;
			display: block;
			margin-bottom: 0.5rem;
		}

		.benefit-item p {
			color: var(--text-secondary);
			line-height: 1.6;
		}

		.benefit-item strong {
			color: var(--primary-neon);
		}

		/* Portfolio Section */
		.portfolio {
			position: relative;
			padding: 6rem 2rem;
			background: linear-gradient(180deg, var(--dark-bg) 0%, rgba(15, 24, 53, 0.8) 100%);
			z-index: 2;
		}

		.portfolio-grid {
			display: grid;
			grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
			gap: 2rem;
			max-width: 1200px;
			margin: 3rem auto;
		}

		.portfolio-item {
			position: relative;
			overflow: hidden;
			border-radius: 12px;
			aspect-ratio: 1;
			cursor: pointer;
			group: portfolio;
		}

		.portfolio-image {
			width: 100%;
			height: 100%;
			background: linear-gradient(135deg, rgba(0, 212, 255, 0.2), rgba(163, 0, 255, 0.2));
			position: relative;
			border: 1px solid var(--border-glow);
			border-radius: 12px;
			transition: all 0.4s ease;
			display: flex;
			align-items: center;
			justify-content: center;
			font-size: 3rem;
			color: var(--primary-neon);
		}

		.branding-case::before { content: '🎨'; }
		.website-case::before { content: '🌐'; }
		.campaign-case::before { content: '📊'; }
		.video-case::before { content: '🎬'; }
		.rebranding-case::before { content: '✨'; }
		.consulting-case::before { content: '💼'; }

		.portfolio-item:hover .portfolio-image {
			background: linear-gradient(135deg, rgba(0, 212, 255, 0.4), rgba(163, 0, 255, 0.4));
			border-color: var(--primary-neon);
			transform: scale(1.1);
			box-shadow: 0 0 30px rgba(0, 212, 255, 0.5);
		}

		.portfolio-overlay {
			position: absolute;
			bottom: 0;
			left: 0;
			right: 0;
			padding: 2rem;
			background: linear-gradient(to top, rgba(10, 14, 39, 0.95), transparent);
			transform: translateY(100%);
			transition: all 0.4s ease;
		}

		.portfolio-item:hover .portfolio-overlay {
			transform: translateY(0);
		}

		.portfolio-overlay h3 {
			font-size: 1.3rem;
			margin-bottom: 0.5rem;
			color: var(--primary-neon);
		}

		.portfolio-overlay p {
			color: var(--text-secondary);
			font-size: 0.95rem;
		}

		/* Consulting Section */
		.consulting {
			position: relative;
			padding: 6rem 2rem;
			background: linear-gradient(180deg, rgba(15, 24, 53, 0.8) 0%, var(--dark-bg) 100%);
			z-index: 2;
		}

		.consulting-cards {
			display: grid;
			grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
			gap: 2rem;
			max-width: 1200px;
			margin: 3rem auto;
		}

		.consulting-card {
			padding: 2.5rem;
			background: rgba(163, 0, 255, 0.05);
			border: 1px solid rgba(163, 0, 255, 0.2);
			border-radius: 12px;
			backdrop-filter: blur(10px);
			transition: all 0.3s ease;
			position: relative;
		}

		.consulting-card::before {
			content: '';
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 4px;
			background: linear-gradient(90deg, var(--primary-neon), var(--secondary-neon), var(--tertiary-neon));
			border-radius: 12px 12px 0 0;
		}

		.consulting-card:hover {
			background: rgba(163, 0, 255, 0.1);
			border-color: var(--tertiary-neon);
			transform: translateY(-8px);
			box-shadow: 0 20px 40px rgba(163, 0, 255, 0.2);
		}

		.card-number {
			font-size: 3rem;
			font-weight: 700;
			background: linear-gradient(135deg, var(--primary-neon), var(--tertiary-neon));
			-webkit-background-clip: text;
			-webkit-text-fill-color: transparent;
			background-clip: text;
			margin-bottom: 1rem;
		}

		.consulting-card h3 {
			font-size: 1.3rem;
			margin-bottom: 1rem;
			color: var(--text-primary);
		}

		.consulting-card p {
			color: var(--text-secondary);
			line-height: 1.6;
		}

		/* Vendor Network Section */
		.vendor-network {
			position: relative;
			padding: 6rem 2rem;
			background: linear-gradient(180deg, var(--dark-bg) 0%, rgba(15, 24, 53, 0.8) 100%);
			z-index: 2;
		}

		.orbit-container {
			max-width: 600px;
			margin: 3rem auto;
			position: relative;
			width: 100%;
			aspect-ratio: 1;
			display: flex;
			align-items: center;
			justify-content: center;
		}

		.orbit-center {
			position: absolute;
			width: 120px;
			height: 120px;
			background: radial-gradient(circle, rgba(0, 212, 255, 0.3), transparent);
			border-radius: 50%;
			display: flex;
			flex-direction: column;
			align-items: center;
			justify-content: center;
			border: 2px solid var(--primary-neon);
			z-index: 10;
			box-shadow: 0 0 30px rgba(0, 212, 255, 0.5);
		}

		.orbit-logo {
			width: 80px;
			height: 80px;
			object-fit: contain;
			filter: brightness(1.2);
		}

		.orbit-center-text {
			font-weight: 700;
			font-size: 0.9rem;
			color: var(--primary-neon);
			text-align: center;
			position: absolute;
			bottom: 10px;
		}

		.orbit-nodes {
			position: absolute;
			width: 100%;
			height: 100%;
		}

		.orbit-node {
			position: absolute;
			width: 100px;
			height: 100px;
			display: flex;
			flex-direction: column;
			align-items: center;
			justify-content: center;
			cursor: pointer;
		}

		.node-1 { top: 0%; left: 50%; transform: translateX(-50%); animation: orbit-motion-1 20s linear infinite; }
		.node-2 { animation: orbit-motion-2 20s linear infinite; }
		.node-3 { animation: orbit-motion-3 20s linear infinite; }
		.node-4 { animation: orbit-motion-4 20s linear infinite; }
		.node-5 { animation: orbit-motion-5 20s linear infinite; }
		.node-6 { animation: orbit-motion-6 20s linear infinite; }

		@keyframes orbit-motion-1 {
			0% { top: 0%; left: 50%; transform: translateX(-50%); }
			100% { top: 0%; left: 50%; transform: translateX(-50%) rotateZ(360deg); }
		}

		@keyframes orbit-motion-2 {
			0% { top: 13.4%; left: 84.3%; }
			100% { top: 13.4%; left: 84.3%; transform: rotateZ(360deg); }
		}

		@keyframes orbit-motion-3 {
			0% { top: 50%; left: 93.3%; transform: translateY(-50%); }
			100% { top: 50%; left: 93.3%; transform: translateY(-50%) rotateZ(360deg); }
		}

		@keyframes orbit-motion-4 {
			0% { bottom: 13.4%; right: 15.7%; }
			100% { bottom: 13.4%; right: 15.7%; transform: rotateZ(360deg); }
		}

		@keyframes orbit-motion-5 {
			0% { bottom: 0%; left: 50%; transform: translateX(-50%); }
			100% { bottom: 0%; left: 50%; transform: translateX(-50%) rotateZ(360deg); }
		}

		@keyframes orbit-motion-6 {
			0% { top: 50%; left: 6.7%; transform: translateY(-50%); }
			100% { top: 50%; left: 6.7%; transform: translateY(-50%) rotateZ(360deg); }
		}

		.node-dot {
			width: 12px;
			height: 12px;
			border-radius: 50%;
			background: var(--primary-neon);
			box-shadow: 0 0 20px var(--primary-neon);
			margin-bottom: 0.5rem;
		}

		.node-name {
			font-size: 0.85rem;
			text-align: center;
			color: var(--text-secondary);
			transition: all 0.3s ease;
		}

		.orbit-node:hover .node-name {
			color: var(--primary-neon);
			font-weight: 600;
		}

		.orbit-node:hover .node-dot {
			transform: scale(1.5);
			background: var(--secondary-neon);
			box-shadow: 0 0 30px var(--secondary-neon);
		}

		.vendor-info {
			max-width: 700px;
			margin: 3rem auto;
			text-align: center;
			padding: 2rem;
			background: rgba(0, 212, 255, 0.05);
			border: 1px solid var(--border-glow);
			border-radius: 12px;
		}

		.vendor-info p {
			margin-bottom: 1.5rem;
			color: var(--text-secondary);
			line-height: 1.8;
		}

		.vendor-info strong {
			color: var(--primary-neon);
		}

		.vendor-cta {
			margin-top: 1rem;
		}

		/* FAQ Section */
		.faq {
			position: relative;
			padding: 6rem 2rem;
			background: linear-gradient(180deg, rgba(15, 24, 53, 0.8) 0%, var(--dark-bg) 100%);
			z-index: 2;
		}

		.faq-container {
			max-width: 800px;
			margin: 3rem auto;
		}

		.faq-item {
			margin-bottom: 1.5rem;
			border-radius: 8px;
			overflow: hidden;
			background: rgba(0, 212, 255, 0.05);
			border: 1px solid var(--border-glow);
			transition: all 0.3s ease;
		}

		.faq-item[open] {
			background: rgba(0, 212, 255, 0.1);
			border-color: var(--primary-neon);
		}

		.faq-question {
			padding: 1.5rem;
			cursor: pointer;
			display: flex;
			justify-content: space-between;
			align-items: center;
			font-weight: 600;
			color: var(--text-primary);
			transition: all 0.3s ease;
			user-select: none;
		}

		.faq-item:hover .faq-question {
			color: var(--primary-neon);
		}

		.faq-question::after {
			content: '▼';
			color: var(--primary-neon);
			font-size: 0.8rem;
			transition: transform 0.3s ease;
		}

		.faq-item[open] .faq-question::after {
			transform: rotate(180deg);
		}

		.faq-answer {
			padding: 0 1.5rem 1.5rem;
			animation: expand 0.3s ease-out;
			color: var(--text-secondary);
			line-height: 1.8;
		}

		@keyframes expand {
			from {
				opacity: 0;
				max-height: 0;
			}
			to {
				opacity: 1;
				max-height: 500px;
			}
		}

		/* Final CTA Section */
		.final-cta {
			position: relative;
			padding: 6rem 2rem;
			min-height: 600px;
			display: flex;
			align-items: center;
			justify-content: center;
			overflow: hidden;
			z-index: 2;
		}

		.cosmic-background-cta {
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			z-index: 0;
		}

		.stars-cta {
			position: absolute;
			width: 2px;
			height: 2px;
			background: white;
			border-radius: 50%;
			box-shadow: 100px 100px white, 200px 150px white, 300px 50px white, 450px 200px white,
						550px 100px white, 150px 300px white, 400px 400px white, 600px 300px white,
						700px 150px white, 800px 400px white, 900px 250px white, 200px 500px white,
						700px 500px white, 500px 600px white, 100px 700px white, 800px 700px white,
						300px 800px white;
			animation: twinkle 3s infinite;
		}

		.nebula-cta {
			position: absolute;
			width: 600px;
			height: 600px;
			background: radial-gradient(circle, rgba(0, 212, 255, 0.2), transparent);
			border-radius: 50%;
			top: 50%;
			left: 50%;
			transform: translate(-50%, -50%);
			filter: blur(80px);
			animation: drift 8s ease-in-out infinite;
		}

		.cta-content {
			position: relative;
			z-index: 1;
			text-align: center;
			display: flex;
			flex-direction: column;
			align-items: center;
			gap: 2rem;
			max-width: 600px;
		}

		.cta-headline {
			font-size: clamp(2rem, 5vw, 2.8rem);
			font-weight: 800;
			background: linear-gradient(135deg, var(--primary-neon), var(--secondary-neon), var(--tertiary-neon));
			-webkit-background-clip: text;
			-webkit-text-fill-color: transparent;
			background-clip: text;
			line-height: 1.2;
		}

		.cta-subheadline {
			font-size: 1.2rem;
			color: var(--text-secondary);
		}

		.cta-buttons {
			display: flex;
			gap: 1.5rem;
			justify-content: center;
			flex-wrap: wrap;
		}

		/* Footer */
		.footer {
			background: rgba(5, 7, 26, 0.95);
			border-top: 1px solid var(--border-glow);
			padding: 4rem 2rem 2rem;
			color: var(--text-secondary);
			z-index: 3;
			position: relative;
		}

		.footer-content {
			display: grid;
			grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
			gap: 3rem;
			max-width: 1200px;
			margin: 0 auto 3rem;
		}

		.footer-col h4 {
			color: var(--text-primary);
			margin-bottom: 1rem;
			font-size: 1rem;
		}

		.footer-col ul {
			list-style: none;
		}

		.footer-col ul li {
			margin-bottom: 0.8rem;
		}

		.footer-col a {
			color: var(--text-secondary);
			text-decoration: none;
			transition: color 0.3s ease;
		}

		.footer-col a:hover {
			color: var(--primary-neon);
		}

		.footer-logo {
			width: 100px;
			height: auto;
			margin-bottom: 1rem;
			filter: brightness(1.2);
		}

		.footer-tagline {
			color: var(--text-secondary);
			margin-bottom: 1rem;
			line-height: 1.6;
		}

		.footer-bottom {
			text-align: center;
			padding-top: 2rem;
			border-top: 1px solid var(--border-glow);
			color: var(--text-secondary);
			font-size: 0.9rem;
		}

		/* Responsive Design */
		@media (max-width: 768px) {
			.hero {
				padding: 1.5rem;
			}

			.glowing-orb-container {
				width: 150px;
				height: 150px;
				margin-bottom: 1rem;
			}

			.glowing-orb {
				width: 100px;
				height: 100px;
			}

			.services-grid,
			.consulting-cards {
				grid-template-columns: 1fr;
			}

			.pipeline-container {
				flex-direction: column;
				gap: 2rem;
			}

			.pipeline-connector {
				flex-direction: column;
				min-width: auto;
				min-height: 30px;
			}

			.connector-line {
				width: 2px;
				height: 30px;
			}

			.cta-buttons {
				flex-direction: column;
				align-items: center;
			}

			.cta-primary,
			.cta-secondary {
				width: 100%;
				max-width: 300px;
			}

			.hero-ctas {
				justify-content: center;
			}

			.footer-content {
				grid-template-columns: 1fr;
			}

			.orbit-container {
				aspect-ratio: auto;
				max-width: 300px;
				margin: 3rem auto;
			}

			.orbit-center {
				width: 100px;
				height: 100px;
			}

			.orbit-logo {
				width: 60px;
				height: 60px;
			}

			.orbit-node {
				width: 80px;
				height: 80px;
			}
		}

		@media (max-width: 480px) {
			.hero-headline {
				font-size: 1.8rem;
			}

			.hero-subheadline {
				font-size: 1rem;
			}

			.section-header h2 {
				font-size: 1.8rem;
			}

			.cta-primary,
			.cta-secondary {
				padding: 0.9rem 2rem;
				font-size: 0.95rem;
			}

			.services-grid {
				gap: 1rem;
			}

			.service-card {
				padding: 1.5rem;
			}

			.portfolio-grid {
				grid-template-columns: 1fr;
			}

			.final-cta {
				min-height: 400px;
				padding: 3rem 1rem;
			}
		}
	</style>

	<script>
		// Smooth scroll behavior for internal links
		document.querySelectorAll('a[href^="#"]').forEach(anchor => {
			anchor.addEventListener('click', function (e) {
				e.preventDefault();
				const target = document.querySelector(this.getAttribute('href'));
				if (target) {
					target.scrollIntoView({
						behavior: 'smooth',
						block: 'start'
					});
				}
			});
		});

		// Particle hover effect for hero
		document.addEventListener('mousemove', (e) => {
			const particles = document.querySelector('.particles');
			if (particles && e.clientY < window.innerHeight / 3) {
				const x = e.clientX / window.innerWidth;
				const y = e.clientY / window.innerHeight;
				particles.style.transform = `translate(${x * 30}px, ${y * 30}px)`;
			}
		});

		// Service card animations on scroll
		const observerOptions = {
			threshold: 0.2,
			rootMargin: '0px 0px -50px 0px'
		};

		const observer = new IntersectionObserver((entries) => {
			entries.forEach(entry => {
				if (entry.isIntersecting) {
					entry.target.style.animation = 'fadeInUp 0.6s ease-out forwards';
				}
			});
		}, observerOptions);

		document.querySelectorAll('.service-card, .portfolio-item, .consulting-card').forEach(el => {
			observer.observe(el);
		});

		// Button click handlers (for future backend integration)
		document.querySelectorAll('.cta-primary, .cta-secondary').forEach(button => {
			button.addEventListener('click', function(e) {
				const text = this.textContent.trim();
				if (text.includes('Book') || text.includes('Call')) {
					// Handle booking
					console.log('Booking clicked');
				} else if (text.includes('See Our Work')) {
					// Scroll to portfolio
					document.querySelector('.portfolio').scrollIntoView({ behavior: 'smooth' });
				} else if (text.includes('Start Your Project')) {
					// Handle project start
					console.log('Start project clicked');
				}
			});
		});

		// Parallax effect on scroll
		let ticking = false;
		window.addEventListener('scroll', () => {
			if (!ticking) {
				window.requestAnimationFrame(() => {
					const scrolled = window.pageYOffset;
					const nebulas = document.querySelectorAll('.nebula');
					nebulas.forEach((nebula, index) => {
						nebula.style.transform = `translateY(${scrolled * (0.3 + index * 0.1)}px)`;
					});
					ticking = false;
				});
				ticking = true;
			}
		});
	</script>
</body>
</html>
